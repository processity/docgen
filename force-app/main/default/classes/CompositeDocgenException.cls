/**
 * Custom exception for composite document generation errors.
 *
 * Used to distinguish composite-specific errors from standard exceptions,
 * enabling better error handling and user messaging in composite document workflows.
 */
public class CompositeDocgenException extends Exception {
    // No additional implementation needed - inherits all Exception behavior
}
